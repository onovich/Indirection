{
  "name": "indirection",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm build && vitest run",
    "test:watch": "vitest",
    "lint": "echo \"lint not configured yet\"",
    "format": "echo \"format not configured yet\""
  },
  "devDependencies": {
    "@indirection/compiler": "workspace:*",
    "@indirection/protocol": "workspace:*",
    "@types/node": "24.13.2",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
